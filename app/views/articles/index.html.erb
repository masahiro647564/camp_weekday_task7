
<% @articles.each do |article| %>
  <div class="mb-5">
    <h3><%= article.title %></h3>
    <h4 class="text-secondary"><%= article.created_at.strftime('%b %d,%Y') %></h4>
    <p>
      <%= article.body.truncate(100) %>
    </p>
    <p>
      <%= link_to "More detail", article_path(article), class: "text-dark" %>
    </p>
  </div>
<% end %>

